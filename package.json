{
  "name": "python-mastery",
  "version": "1.0.0",
  "private": true,
  "homepage": "/",
  "dependencies": {
    "ajv": "^8.18.0",
    "cross-env": "^10.1.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "5.0.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "build:gh": "cross-env PUBLIC_URL=/python-mastery react-scripts build",
    "predeploy": "npm run build:gh",
    "deploy": "gh-pages -d build"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead"
    ],
    "development": [
      "last 1 chrome version"
    ]
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}
